version: '3.8'

services:
  web:
    build:
      context: ./dist
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
    volumes:
      - ./renders:/app/public/renders:ro
    environment:
      - NODE_ENV=production
